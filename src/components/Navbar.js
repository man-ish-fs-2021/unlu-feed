import React from "react";
import logo from "../unlu-logo.png";

const Navbar = () => {
  return (
    <div className="navbar">
      <img src={logo} alt="Unlu"></img>
    </div>
  );
};

export default Navbar;
